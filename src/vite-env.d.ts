/// <reference types="vite/client" />

// Provides the `import.meta.glob` typings that Vite resolves at
// build time. The types are erased at compile time — only the
// `vite/client` module's type augmentations on `ImportMeta` matter
// at the TypeScript level.
